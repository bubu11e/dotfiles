# One unlock per working day: the default TTL renews on each use, and the max is
# the hard ceiling regardless of activity, so an unattended machine re-locks
# overnight. Requires `gpgconf --reload gpg-agent` to take effect.
default-cache-ttl 28800
max-cache-ttl 86400
