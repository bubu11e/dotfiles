{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track each vendored Claude skill upstream's main HEAD",
      "managerFilePatterns": ["/^scripts/update-vendored-skills\\.sh$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+) packageName=(?<packageName>\\S+) branch=(?<currentValue>\\S+)\\s+[A-Z0-9_]+_REF=\"(?<currentDigest>[0-9a-f]+)\""
      ]
    }
  ],
  "postUpgradeTasks": {
    "commands": ["bash scripts/update-vendored-skills.sh"],
    "fileFilters": ["home/dot_claude/skills/**"],
    "executionMode": "branch"
  }
}
